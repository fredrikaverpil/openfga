# Test Commit

This file was created as a test commit to verify the Claude Code session
workflow (branch creation, commit, and push). It has no effect on the
OpenFGA codebase and can be safely deleted.
